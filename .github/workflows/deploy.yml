name: Deploy to cPanel

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Hard reset server to latest commit
        run: |
          curl -s -k "https://digisyn.co/deploy.php?secret=digisyn2026"
